# Role: 顶尖 UI 设计大师 & 资深前端开发工程师

## 1. 任务目标
我将提供一个 Vue 3 (`<script setup>`) 页面的基础代码或片段。请你完全接管该页面的 UI 设计与前端代码重构。
你需要忽略复杂的底层业务逻辑，**使用纯前端死数据（Mock Data）**将页面填充完整。你的唯一核心目标是：**打造一个极致美观、清新、拥有 Apple App 级高级质感的移动端 Web 页面。**

## 2. 项目背景与定位
这是一个移动端优先（Mobile-First）的轻量化“家庭健康辅助工具”，专注“家庭药箱”垂直场景。
- **受众：** 家庭成员，包含中老年人（需要大字体、高对比度、清晰防误触）。
- **核心体验：** 消除复杂的表单，用最直观的卡片、打卡交互、色彩反馈来降低管理门槛。
- **调性：** 清新（Fresh）、治愈（Healing）、克制（Minimalist）、安全可靠（Trustworthy）。

## 3. 全局 UI 设计规范 (Apple App 风格)
在重构代码时，必须严格遵守以下设计规范，强烈建议使用 **Tailwind CSS** 原子类来实现：

### 3.1 色彩系统 (Color Palette)
- **背景色：** 极简的浅灰背景 `bg-gray-50` 或 `bg-[#F2F2F7]`，让内容卡片凸显。
- **卡片底色：** 纯白 `bg-white`。
- **主色调 (Primary)：** Apple 风格的科技蓝 `text-blue-500` (`#007AFF`)，用于关键按钮和强调文字。
- **状态色 (Status)：** 
  - 成功/健康（打卡完成）：柔和的绿色 `text-emerald-500` (`#34C759`)。
  - 警告/临期：柔和的橙色 `text-orange-500` (`#FF9500`)。
  - 危险/过期：醒目的红色 `text-red-500` (`#FF3B30`)。
- **文字层级：** 标题 `text-gray-900`，副标题/备注 `text-gray-500`，禁用态 `text-gray-300`。

### 3.2 质感与形状 (Texture & Shapes)
- **大圆角 (Squircle)：** 卡片使用 `rounded-2xl` 或 `rounded-3xl`；按钮使用 `rounded-full` 或 `rounded-xl`，杜绝直角。
- **高级阴影 (Soft Shadows)：** 抛弃生硬的黑影，使用极其柔和的弥散阴影，例如 `shadow-[0_8px_30px_rgb(0,0,0,0.04)]`。
- **毛玻璃效果 (Glassmorphism)：** 底部导航、悬浮按钮或顶部导航栏，使用 `backdrop-blur-md bg-white/80` 增加空间层次感。

### 3.3 交互与动画 (Micro-interactions)
- **点击反馈：** 所有可点击元素（卡片、按钮）必须有按下时的缩放反馈 `active:scale-[0.98] transition-transform duration-200`。
- **平滑过渡：** 颜色切换、展开收起必须有 `transition-all duration-300 ease-in-out`。

### 3.4 控件与排版细节
- **大触控区：** 考虑到中老年人，列表项和按钮的高度至少 `h-14`，内边距充足 `p-4` 或 `p-5`。
- **图标 (Icons)：** 使用线条圆润的无衬线图标（建议采用 Lucide Icons 或 Heroicons 风格的 SVG），图标大小适中，图标配合背景色块（如 `bg-blue-50 text-blue-500 rounded-full p-2`）显得更精致。
- **无感表单 (Seamless Forms)：** 表单输入框不要用生硬的边框，使用浅灰色背景 `bg-gray-100`，圆角 `rounded-xl`，去除 focus 时的 outline，改为柔和的 ring。

---

## 4. 当前页面定制化要求：【用药提醒 (Pill Reminder)】
当前页面旨在管理和展示用户的服药任务。请基于我提供的代码片段，实现以下特定 UI 设计：

1. **页面头部 (Header)：**
   - 大标题排版，左对齐 "用药提醒"，下方可带一句温馨副标题（如："今天也要按时吃药哦"）。
   - 包含一个清新的进度概览（例如：今日完成 2/4）。

2. **时间轴/卡片列表 (Timeline / Task Cards)：**
   - 以时间顺序排列打卡任务。
   - **未吃药状态：** 醒目的时间字体，配有 iOS 风格的 Switch 开关（纯 CSS/SVG 实现，绿色代表开启）。
   - **已吃药状态：** 视觉降级（文字变灰，卡片透明度降低），配有一个优雅的绿色 Checkmark 动画或徽章。
   - 卡片内部排版：左侧显示时间和服药人，中间显示药品名称和剂量，右侧为打卡按钮或开关。

3. **悬浮添加按钮 (FAB) 或 底部固定操作栏：**
   - 屏幕右下角提供一个带有阴影和毛玻璃效果的悬浮大按钮（`+`），或底部一个宽大的 "添加提醒" 按钮。

4. **优雅的半屏弹窗 (Bottom Sheet for Add/Edit)：**
   - 点击添加时，不要跳转页面，而是从底部弹出一个带遮罩层的半屏弹窗（Bottom Sheet）。
   - 弹窗顶部居中有一个灰色的拖拽指示条（小短线 `w-12 h-1.5 bg-gray-300 rounded-full`），极具 iOS 原生感。
   - 弹窗内的表单要极其简洁（药品名称输入、服药人选择、时间选择器模拟、备注输入）。

## 5. 输出要求
- 输出**完整**的 Vue 3 SFC (`.vue` 文件) 代码。
- 采用 `<script setup lang="ts">`，使用组合式 API。
- HTML 结构中直接写满 Tailwind CSS 类名。
- 请补充完整的 Mock 数据，确保页面一运行就能看到极度美观的满状态（包含已打卡、未打卡、不同成员的数据）。
- 必要的图标直接使用 SVG 代码内联（精简、圆润风格），不要依赖外部组件库加载。

现在，请接收我提供的页面代码，并开始你的设计与重构：